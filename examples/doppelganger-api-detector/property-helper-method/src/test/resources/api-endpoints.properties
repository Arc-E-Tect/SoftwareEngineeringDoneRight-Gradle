# Single source of truth for this example's request paths - both ApiEndpoints (loaded at test
# runtime via the classpath) and the Doppelganger plugin's propertyFiles configuration (loaded at
# build time, out of band) read this same file.
users.by-username=/v1/users/{username}
