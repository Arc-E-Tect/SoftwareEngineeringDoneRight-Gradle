openapi: 3.1.0
info:
  title: Catalog API
  version: "1.0"
paths:
  /items/{id}:
    get:
      operationId: getItem
      tags: [Items]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string
  /items:
    get:
      operationId: listItems
      tags: [Items]
      responses:
        '200':
          description: OK
